# README

```
此项目提供了基于 “Microsoft.Extensions.Logging” 的，记录运行时日志的服务。此项目仅适用于 “支持 .NET Standard 2.0” 的项目。

This project provides a service based on "Microsoft.Extensions.Logging" that records runtime logs. This project is only available for projects that support .NET Standard 2.0.
```

